DQL SELECT DISTINCT ROWID ROWNUM TO_DATE('1990-10-11','YYYY-MM-DD') TO_CHAR(SYSDATE,'YYYY/MM/DD') MAX MIN AVG SUM COUNT DISTINCT 
FROM WHERE GROUP BY HAVING ORDER BY ASC DESC

DDL CREATE DROP ALTER TRUNCATE
CREATE TABLE ALTER TABLE DROP TABLE
CREATE VIEW CREATE OR REPLACE VIEW DROP VIEW
CREATE SEQUENCE ALTER SEQUENCE DROP SEQUENCE
CREATE USER
NUMBER VARCHAR2 DATE BLOB CLOB
CONSTRAINT PRIMARY KEY CHECK NOT NULL UNIQUE FOREIGN KEY REFERENCES DEFAULT
CURRVAL NEXTVAL START WITH INCREMENT BY MAXVALUE MINVALUE CACHE CYCLE

DML INSERT INTO UPDATE WHERE DELETE [FROM] WHERE 
DELETE TRUNCATE DROP FOR UPDATE

DCL GRANT REVOKE CONNECT RESOURCE COMMIT ROLLBACK

PL/SQL SET SERVEROUT ON DECLARE BEGIN RAISE EXCEPTION WHEN DUP_VAL_ON_INDEX THEN WHEN TOO_MANY_ROWS THEN WHEN NO_DATA_FOUND THEN WHEN OTHERS THEN END; DBMS_OUTPUT.PUT_LINE IF THEN END IF; IF THEN ELSE END IF; IF THEN ELSIF THEN END IF;IF THEN ELSIF THEN ELSE END IF; CASE WHEN THEN WHEN THEN ELSE END LOOP EXIT WHEN END LOOP; WHILE LOOP END LOOP; FOR LOOP END LOOP; CURSOR IS SELECT FROM WHERE GROUP BY HAVING ORDER BY ASC DESC OPEN FETCH INTO CLOSE TYPE %TYPE %ROWTYPE %NOTFOUND %FOUND %ROWCOUNT %ISOPEN 